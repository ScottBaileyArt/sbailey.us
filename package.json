{
  "name": "sbailey-us",
  "private": true,
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "rm -rf _site && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
